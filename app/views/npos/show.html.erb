<p id="notice"><%= notice %></p>

<% content_for :markup do %>
  <script type="application/ld+json">
{
    "@context" : "https://schema.org",
    "@type" : "Organization",
    "name" : "<%= @npo.corporate_name %>",
    "address": "<%= @npo.address %>",
    "founder":"<%= @npo.representative %>",
    "foundingDate":"<%= @npo.certification_date %>", 
    "description" : "<%= @npo.establish_purpose %>"
}
</script>
<% end %>

<% content_for :title do %>
  <%= @npo.corporate_name + "を詳しく紹介！広報・採用・情報発信なら | PR-LABO" %>
<% end %>
<% breadcrumb :npo, @npo %>

<h1 class="companytitle"><%= @npo.corporate_name %></h1>
<br>

<div class="box27">
    <span class="box-title">法人紹介</span>
    <p><%= simple_format(@npo.pr) %></p>
</div>

<p>
  <%= image_tag @npo.img.url if @npo.img? %>
</p>

<table class="table03">
  <tr>
    <th>会社名</th>
    <td><%= @npo.corporate_name %></td>
  </tr>
  <tr>
    <th>住所</th>
    <td><%= @npo.address %></td>
  </tr>
  <tr>
    <th>代表者名</th>
    <td><%= @npo.representative %></td>
  </tr>
  <tr>
    <th>設立認証年月日</th>
    <td><%= @npo.certification_date %></td>
  </tr>
  <tr>
    <th>定款に記載された目的</th>
    <td><%= @npo.establish_purpose %></td>
  </tr>
  <tr>
    <th>分野</th>
    <td><%= @npo.field %></td>
  </tr>
</table>


<div class="parent clearfix">
<% if params[:id].to_i > 1 %>
    <div class="left"><%= link_to '前のページへ', npo_path(params[:id].to_i - 1) %></div>
<% end %>
<% if params[:id].to_i < 29478 %>
    <div class="right"><%= link_to '次のページへ', npo_path(params[:id].to_i + 1) %></div>
<% end %>
</div>
<br>
<br>
<%= link_to 'NPO法人一覧へ', npos_path %>
<br>
<br>
<br>

<h1>お問い合わせ内容送信フォーム</h1>
<br>
<p>PR-LABOは「企業の魅力を発信する」Webサービスです。</p>
<p>下記フォームから問い合わせをして頂くことにより、企業のページ内容の更新・充実化ができます。</p>
<p>他にも質問や不明点などあれば何なりとお問い合わせください。</p>
<br>
<%= form_with url: mail_path, local: true do |f| %>
  <div class="field">
    <%= f.label :法人名 %>
    <%= f.text_field :corporate_name, value: @npo.corporate_name %>
  </div>
  <div class="field">
    <%= f.label :お名前 %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :メールアドレス %>
    <%= f.text_field :email %>
  </div>
    <div class="field">
    <%= f.label :お問い合わせ内容 %>
    <%= f.text_area :text, :size => "80x15" %>
  </div>
  <div class="actions">
  <%= f.submit "送信" %>
  </div>
<% end %>


