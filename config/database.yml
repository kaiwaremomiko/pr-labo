# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("heroRAILS_MAX_THREADS") { 5 } %>
  username: sunsukeyo1
  password: 0719
  host: localhost
  timeout: 5000

development:
  <<: *default
  database: prlabo_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: prlabo_test

production:
  <<: *default
  database: prlabo_production
  username: prlabo
  password: <%= ENV['PRLABO_DATABASE_PASSWORD'] %>
